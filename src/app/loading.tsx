import { LoadingState } from "@/components/feedback/page-state";

export default function RootLoading() {
  return <LoadingState title="Loading Fantasia" description="Preparing the private workspace shell." />;
}
